<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="me.ranieripieper.android.github.gitrepository">

    <dist:module
        dist:instant="false"
        dist:title="@string/title_gitrepository">
        <dist:delivery>
            <dist:install-time />
        </dist:delivery>
        <dist:fusing dist:include="true" />
    </dist:module>

    <uses-permission android:name="android.permission.INTERNET" />

    <application>
        <activity
            android:name="me.ranieripieper.android.github.gitrepository.view.GitRepositoryActivity"
            android:theme="@style/Theme.GitHub.NoActionBar" />
    </application>
</manifest>